import { AppAlertDirective } from './app-alert.directive';

describe('AppAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAlertDirective();
    expect(directive).toBeTruthy();
  });
});
